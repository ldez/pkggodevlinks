// Package bir is an example to add links into pkg.go.dev (package level).
//
// # Links
//
// - Title from godoc (pkg bir) 1, https://example.com
// - Title from godoc (pkg bir) 2, https://example.org
package bir

import "fmt"

func Hello(msg string) {
	fmt.Println(msg)
}
